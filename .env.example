# 이 파일을 복사해 .env.local 로 이름을 바꾼 뒤 값을 채워 사용하세요. (.env.local 은 절대 커밋 금지)

# server/result/claude.service.js 에서 사용. 비워두면 로컬 규칙 기반 매칭으로 자동 폴백합니다.
ANTHROPIC_API_KEY=

# server/index.js 가 사용할 포트 (기본값 3000)
PORT=3000
